# ═══════════════════════════════════════════════════════════════════════
# ImmunOmics — CI/CD Pipeline
# ═══════════════════════════════════════════════════════════════════════
# Runs linting (ruff) and tests (pytest) on every push/PR to main.
# ═══════════════════════════════════════════════════════════════════════

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11"]

    env:
      MPLBACKEND: Agg

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Show dependency versions
        run: |
          python -c "import scanpy; print('scanpy', scanpy.__version__)"
          python -c "import anndata; print('anndata', anndata.__version__)"
          python -c "import numpy; print('numpy', numpy.__version__)"

      - name: Lint with ruff
        run: |
          ruff check immunomics/ tests/

      - name: Format check with ruff
        run: |
          ruff format --check immunomics/ tests/

      - name: Run tests
        run: |
          python -m pytest tests/ -v --tb=long
